import { ClientsearchPipe } from './clientsearch.pipe';

describe('ClientsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
