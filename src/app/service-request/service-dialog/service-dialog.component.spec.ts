// import { HttpClientModule } from '@angular/common/http';
// import { ComponentFixture, TestBed } from '@angular/core/testing';
// import { FormsModule, ReactiveFormsModule } from '@angular/forms';
// import { MatDialogModule, MatDialogRef, MAT_DIALOG_DATA } from '@angular/material/dialog';
// import { MatFormFieldModule } from '@angular/material/form-field';
// import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

// import { ServiceDialogComponent } from './service-dialog.component';

// describe('ServiceDialogComponent', () => {
//   let fixture: ComponentFixture<ServiceDialogComponent>;

//   beforeEach(async () => {
//     await TestBed.configureTestingModule({
//       declarations: [ ServiceDialogComponent ],
//       imports: [
//         ReactiveFormsModule,
//         FormsModule,
//         HttpClientModule,
//         MatDialogModule,
//         BrowserAnimationsModule,
//         MatFormFieldModule
//       ],
//       providers: [
//         { provide: MAT_DIALOG_DATA, useValue: {} },
//         { provide: MatDialogRef, useValue: {} }
//       ]
//     })
//     .compileComponents();

//     fixture = TestBed.createComponent(ServiceDialogComponent);
//   });

  // it('should create', () => {
  //   const app = fixture.debugElement.componentInstance;
  //   expect(app).toBeTruthy();
  // });

  // it('should have update form', () => {
  //   fixture.detectChanges();
  //   const app = fixture.debugElement.nativeElement as HTMLElement;
  //   expect(app.querySelector('#requestUpdateForm')).toBeTruthy();
  // })

  // it('should have header', () => {
  //   fixture.detectChanges();
  //   const app = fixture.debugElement.nativeElement as HTMLElement;
  //   expect(app.querySelector('#updateHeader')?.textContent).toContain('Update Service Request');
  // })
// });
