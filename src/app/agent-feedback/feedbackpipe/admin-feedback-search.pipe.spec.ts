import { AdminFeedbackSearchPipe } from './admin-feedback-search.pipe';

describe('AdminFeedbackSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminFeedbackSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
